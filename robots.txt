# NOTE: this file only has any effect if the site is served from a domain ROOT.
# The current preview lives in a subdirectory, where crawlers ignore it, so the
# real protection is the noindex meta tag in every page's <head>.
#
# When moving to the live domain: delete this file AND remove the noindex meta
# tag from every page, or the site will never appear in search results.

User-agent: *
Disallow: /
